org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true
kotlin.code.style=official

# O alvo Android exige o SDK instalado; sem ele o Gradle nem configura o
# projeto. Ligue quando for abrir no Android Studio:
#
#   ./gradlew -Pfintips.android=true build
#
# O código compartilhado não muda — é só mais um alvo lendo o mesmo commonMain.
fintips.android=false
